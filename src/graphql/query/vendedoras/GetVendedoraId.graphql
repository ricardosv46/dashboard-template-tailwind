query GetVendedoraId($vendedoraId: Int) {
  GetVendedoraId(vendedoraId: $vendedoraId) {
    vendedoraId
    nombres
    apellidos
    link
    estado
    imagenPrincipal {
      id
      titulo
      estado
      url
    }
  }
}
