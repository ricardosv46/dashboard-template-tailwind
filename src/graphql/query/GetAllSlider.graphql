query GetAllSlider($estado: String) {
	GetAllSliders(estado: $estado) {
		numeroTotal
		data {
			sliderId
			titulo
			tipoLink
			link
			estado
			imagenPrincipal {
				id
				titulo
				url
			}
		}
	}
}
