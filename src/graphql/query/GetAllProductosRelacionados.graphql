query GetAllProductosRelacionados($slug: String) {
  GetAllProductosRelacionados(slug: $slug) {
    numeroTotal
    data {
      productoId
      titulo
      imagenPrincipal {
        url
      }
    }
  }
}
